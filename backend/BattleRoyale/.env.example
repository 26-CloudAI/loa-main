# 보스봇 GCS 가중치 URI (gs://버킷명/경로)
# 설정하지 않으면 로컬 bots/trained_weights.json 사용
BOSS_WEIGHTS_GCS_URI=

# GCP 서비스 계정 키 경로 (GCE VM이면 불필요 — VM 첨부 계정으로 자동 인증)
# GOOGLE_APPLICATION_CREDENTIALS=/path/outside/repo/service-account.json
