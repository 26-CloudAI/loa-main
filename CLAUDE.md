# AI Arena — 프로젝트 공통 규칙

모든 에이전트와 Claude Code는 아래 규칙을 반드시 준수한다.

## 코드 작성 규칙

1. **코드 작성 후 반드시 실행 점검**: 모든 코드를 작성하거나 수정한 뒤에는 프로젝트를 직접 실행해서 정상 동작을 확인한다. 오류가 있으면 수정까지 완료해야 작업이 끝난 것이다.

2. **환경 독립성 보장**: 어떤 운영체제(Linux, macOS, Windows)와 환경에서도 프로젝트가 정상 작동해야 한다. 경로 구분자, 파일 인코딩, 줄바꿈 문자 등 OS별 차이를 고려해서 작성한다.

3. **출력 최소화**: 출력은 필요한 내용만 출력한다. 디버그 로그, 불필요한 진행 메시지, 중복 출력을 남기지 않는다.

## 작업 공통 원칙

- 수정 전 반드시 관련 파일을 읽고 기존 코드를 파악한다.
- 요청 범위를 벗어난 리팩토링, 기능 추가, 주석 추가를 하지 않는다.
- 테스트가 있는 영역을 수정할 때는 해당 테스트가 통과하는지 확인한다.
- 밸런스 수치는 `src/arena/config.py` 한 파일에서만 관리한다.
