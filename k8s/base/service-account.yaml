apiVersion: v1
kind: ServiceAccount
metadata:
  name: game-server
  namespace: arena
  annotations:
    # Phase 6에서 실제 GCP SA 이메일로 교체
    iam.gke.io/gcp-service-account: game-server-sa@knu-2026-sungjin0418.iam.gserviceaccount.com
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bot-runner
  namespace: arena
  # bot-runner는 GCP 리소스 접근 권한 없음 — annotation 없음
