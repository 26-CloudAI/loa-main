# League of Agents

---

## 1. 개요

---

League of Agents는 사용자가 직접 작성한 봇 로직을 제출하고, 여러 봇이 게임의 승리 목표를 달성하는 과정을 관전할 수 있는 멀티플레이어 AI Agent 플랫폼이다.

이 통합 PRD는 현재 프로젝트의 단일 기준 문서 역할을 하도록 작성되었다. 이 문서는 다음을 함께 반영한다.

- 기존 PRD와 게임 룰 문서에서 정리된 방향성
- 현재 반영된 실제 구현 상태
- 이미 구현된 범위와 아직 계획 단계인 범위의 명확한 구분

## **2. 제품 목표**

---

제품은 다음 가치를 제공해야 한다.

- 사용자가 자신의 봇 코드를 안전하게 실행할 수 있는 신뢰성 있는 환경
- 전략 다양성이 살아있는, 결정적이고 재현 가능한 플랫폼 제공
- 개발자와 관전자를 위한 가시성이 뛰어난 게임 과정과 결과
- 향후 확장할 수 있는 기반

## 3. 현재 기준 비목표

---

다음 항목은 별도 일정으로 확정되지 않는 한 현재 기준 필수 범위로 보지 않는다.

- 프로덕션 수준의 완전한 인증/인가 체계
- 완성형 UI 및 고도화된 프론트엔드 제품화
- LLM 해설 기능의 필수 릴리스 포함
- 분산 Worker Node 배포 구조
    
    ### 3-2. 향후 확장성
    
    다음 항목은 필수 범위는 아니지만 향후 확장할 대상으로 본다.
    
    - RL 보스 모드, PVE 모드
    - 모의 주식 모드 / 하네스 모드 / 팀플레이 모드
    - 자원 전달 행동, 고급 지형 시스템

## 4. 타겟 사용자

---

### 4.1 봇 개발자

목적:

- 간단한 action(state) 인터페이스로 봇 작성
- 봇을 제출하거나 게임에 투입시킴
- 결과를 피드백하면서 전략 개선
- 경기를 실시간으로 시청
- 리더보드 갱신 확인

### 4.2 운영자 / 유지보수 담당자

목적:

- 악성 코드로부터 서버 보호
- 경기 세션 및 실패 상황 관찰
- 엔진 코어를 유지한채 점진적으로 플랫폼 확장

## 5. 핵심 제품 정의

---

AI Arena는 다음과 같이 정의하는 것이 가장 적절하다.

> 부분 관측 기반 AI Agents 전략 게임
> 

이 경험은 다음 요소를 결합한다.

- 프로그래밍 게임형 봇 설계
- 경쟁 / 목적 달성
- 전투, 확장 시 Agents 간 협업
- 실시간 관전

## 6. 게임 규칙

---

이 섹션은 v1.0.0 게임 룰의 기준 명세서이며, 현재 엔진 코드와 일치하도록 정의한다.

### 6.1. 배틀로얄 맵

- 맵 크기: `100` x `100`
- 초기 광물 수: `300`
- 중앙 고밀도 광물 구역: `30` x `30`
- 코너 희귀 광물 구역 크기: `12` x `12`
- 희귀 광물 비율: `30`
- 광물 재생:
    - 채굴 후 재생 가능 시점까지 `50` 틱 필요
    - 재생 확률: `15%`

### 6.2. 배틀로얄 봇

- 설정상 최대 봇  수: `100`
- 초기 에너지: `100`
- 코너 기준 스폰 여유 거리: `15`
- 시야 반경: `2`
    - 실제 시야 크기: `5 X 5`

### 6.3 배틀로얄 행동

봇은 아래 `11`개 행동 문자열 중 하나를 반환해야 한다.

- `STAY`
- `MOVE_UP`
- `MOVE_DOWN`
- `MOVE_LEFT`
- `MOVE_RIGHT`
- `MINE`
- `ATTACK_UP`
- `ATTACK_DOWN`
- `ATTACK_LEFT`
- `ATTACK_RIGHT`
- `SHIELD`

### 6.4 행동 비용

다음은 봇들의 행동에 따른 코스트이다.

- `STAY`: `-1`
- `MOVE_*`: `-2`
- `MINE`: `-3`
- `ATTACK_*`: `-5`
- `SHIELD`: `-3`

### 6.5 전투

다음은 전투 관련 규칙이다.

- 공격 피해량: `25`
- 실드 피해 감소율: `50%`
- 공격은 방향 기반이며 인접 칸을 대상으로 한다
- 공격이 빗나가도 에너지는 소모된다
- 동시 공격 상황은 엔진 tick 처리 순서에 따라 해석된다

### **6.6 채굴과 점수**

다음은 채굴과 최종 점수에 관한 규칙이다.

- 일반 광물 점수: `+15`
- 희귀 광물 점수: `+40`
- 경합 채굴은 설정 비율에 따라 점수를 나눠 가진다
- 생존 보너스: 생존 tick당 `+0.1`
- 처치 보너스: 킬당 `+10`

최종 랭킹 점수는 다음과 같다.

`광물 점수 + 생존 tick * 0.1 + 킬 수 * 10`

### 6.7 자기장 시스템

다음은 자기장 시스템 관련 규칙이다.

- Phase 1:
    - Tick 구간: `0-299`
    - 자기장 수축 없음
- Phase 2:
    - Tick 구간: `300-449`
    - `20`틱마다 `1`칸 수축
    - 자기장 밖 피해: `3`
- Phase 3:
    - Tick 구간: `450-500`
    - `10`틱마다 `1`칸 수축
    - 자기장 밖 피해: `3`

### 6.8 경기 종료 조건

다음 중 하나가 만족되면 경기가 종료된다.

- 생존 봇이 1개만 남았을 때
- 최대 tick 수에 도달했을 때
- 엔진이 설정된 규칙에 따라 게임 종료를 판정했을 때

최대 틱 수: `500`

### 6.9 Tick 처리 순서

게임 엔진은 각 tick을 다음 순서로 처리한다.

1. 행동 수집
2. 이전 실드 제거
3. 실드 행동 적용
4. 공격 해석
5. 이동 해석
6. 채굴 해석
7. 대기 비용 적용
8. 자기장 피해 적용
9. 사망 처리
10. 생존 tick 갱신
11. 광물 재생
12. tick 증가 및 자기장 갱신
13. 게임 종료 조건 확인

이 순서는 공정성과 결정성을 보장하는 핵심 규칙이므로 쉽게 변경하면 안 된다.

## 7. 봇 인터페이스 계약

---

봇은 다음 형태를 구현하는 것을 기준으로 한다.

```python
def action(state: dict) -> str:
		...
```

### 7.1 State 구조

봇이 받는 상태 정보에는 다음이 포함된다.

- `tick`
- `my_bot`
    - `id`
    - `position`
    - `energy`
    - `score`
    - `shield_active`
- `vision`
    - `5 x 5` 로컬 시야 `grid`
- `zone_boundary`
- `leaderboard`

봇은 전역 맵 전체를 알 수 없고, 부분 관측 정보만 받는다.

## 8. 예시 전략 아키타입

---

프로젝트에는 현재 다음과 같은 예시 봇 전략이 포함되어 있다.

- Herbivore:
    - 채굴 중심
    - 필요 시 적을 회피
- Mad Dog:
    - 공격 중심
    - 에너지가 낮을 때만 채굴을 보조적으로 사용
- Camper:
    - 생존 중심
    - 안전, 방어, 후반 운영을 균형 있게 추구

이 봇들은 데모용일 뿐 아니라 사용자 온보딩과 콜드스타트 생태계 구성에도 유용하다.

## 9. 백엔드 아키텍처

---

### 9.1 코어 계층

- 엔진 계층:
    - 게임 설정
    - 공용 타입
    - Grid, Zone, Vision, Engine
- 서버 계층:
    - FastAPI 앱
    - 게임 세션
    - WebSocket 관전자 흐름
    - 상태 저장소 및 pub/sub 추상화
- 샌드박스 계층:
    - Docker 컨테이너 실행 경로
    - 사용자 봇 코드용 래퍼 서버
    - 어댑터 및 풀 관리
- DB 계층:
    - SQLite 스키마
    - 사용자, 봇, 게임 repository

### 9.2 현재 런타임 모드

현재 코드 기준으로는 개념적으로 두 가지 실행 모드가 존재한다.

- In-Process 모드:
    - 현재 FastAPI 앱 경로에서 사용되는 방식
    - 제출된 코드를 Python `exec`로 같은 프로세스 안에서 실행
    - 로컬 개발 또는 임시 테스트 용도로만 적절함
- Sandbox 모드:
    - Docker 기반 격리 실행을 목표로 설계된 경로
    - 신뢰할 수 없는 사용자 코드 실행을 위한 안전한 방식으로 의도됨

### 9.3 아키텍처 원칙

장기적으로 의도하는 프로덕션 구조는 다음과 같다.

- 엔진이 룰과 상태 진행의 단일 권위 주체가 된다.
- 사용자 봇은 격리된 컨테이너에서 실행된다.
- 실시간 경기 상태는 관전자에게 발행된다.
- 저장소에는 사용자, 봇, 경기, 결과가 남는다.

## 10. 실시간 관전

---

플랫폼은 WebSocket 기반 실시간 경기 관전을 지원해야 한다.

### **10.1 Tick 브로드캐스트 데이터**

각 tick 브로드캐스트에는 다음 정보가 포함되어야 한다.

- 현재 tick 번호
- 봇 위치 및 상태
- 광물 위치
- 자기장 경계
- 생존 봇 수
- 리더보드

### **10.2 이벤트 브로드캐스트 데이터**

다음과 같은 주요 이벤트를 전송할 수 있어야 한다.

- 킬 이벤트
- 채굴 성공 이벤트
- 사망 이벤트
- 게임 시작 / 게임 종료 알림

## **11. 데이터 모델**

DB 계층은 다음 엔터티를 기준으로 설계되어 있다.

- `users`
    - 계정 정보와 상태
- `bots`
    - 저장된 봇 코드와 메타데이터
    - 버전 관리 및 승패 카운터 포함
- `games`
    - 경기 라이프사이클 상태와 결과 요약
- `game_participants`
    - 경기별 각 봇의 참가 기록

이 스키마를 현재 기준 영속성 모델의 기본 형태로 본다.

## **12. API 범위**

### **12.1 현재 구현 / 부분 구현 범위**

현재 서버 측 범위에는 다음이 포함된다.

- 게임 생성
- 활성 게임 목록 조회
- 게임 정보 조회
- 게임 결과 조회
- 게임 삭제
- WebSocket 기반 게임 관전

### **12.2 계획되어 있으나 아직 종단 간 완성되지 않은 범위**

다음은 제품 방향에는 포함되지만 아직 완전하게 연결되지 않은 기능이다.

- 봇 CRUD 엔드포인트
- 완전한 인증/인가
- DB 기반 봇 제출 워크플로우
- 경기 이력 UI 및 랭킹 UI
- 프로덕션 환경에서의 샌드박스 강제 적용
- 분산 Worker Node 실행 구조
- LLM 해설 연동

## **13. 보안 요구사항**

### **13.1 제품이 지향하는 필수 원칙**

신뢰할 수 없는 사용자 코드는 최종적으로 제한된 샌드박스 안에서만 실행되어야 한다.

필수 제어 항목:

- CPU 제한
- 메모리 제한
- PID 제한
- 가능하다면 읽기 전용 루트 파일시스템
- Linux capability 제거
- 행동 요청당 timeout
- 실패 시 `STAY`로 안전하게 fallback

## **14. 비기능 요구사항**

### **14.1 결정성과 공정성**

- Tick 처리 순서는 결정적이어야 한다
- 공유 규칙 계산은 엔진이 중앙에서 수행해야 한다
- 봇은 제공된 부분 상태만으로 행동해야 한다

### **14.2 성능**

기본 목표:

- 관전 가능한 수준의 실시간 게임 진행
- 샌드박스 실행 기준 봇 응답 timeout은 `100ms` 수준
- 컨테이너 시작 시간은 수 초 내 유지

### **14.3 안정성**

- 개별 봇 실패가 게임 엔진 전체를 죽이면 안 된다
- timeout과 실행 오류는 안전한 기본 행동으로 강등되어야 한다
- 경기 상태 발행과 결과 저장은 일반적인 세션 오류 상황에서도 최대한 유지되어야 한다

### **14.4 확장성**

코드베이스와 제품은 엔진 코어 전체를 다시 쓰지 않고도 향후 기능을 추가할 수 있어야 한다.

## **15. MVP 정의**

현재 코드베이스 기준 실질적인 MVP는 다음과 같다.

- v1.0 규칙을 반영한 게임 엔진
- 예시 봇들
- 로컬 시뮬레이션 실행기
- FastAPI 기반 게임 세션 관리
- WebSocket 기반 실시간 관전
- 사용자, 봇, 게임을 위한 기본 영속성 모델
- 향후 연동 가능한 Docker 샌드박스 모듈

다음 항목은 유용하지만 MVP의 필수 조건은 아니다.

- 완전한 인증 흐름
- 완전한 봇 관리 API
- LLM 해설
- RL 보스 봇
- 프로덕션 분산 배포

## **16. 로드맵**

### **16.1 단기**

- 서버 경로에서 in-process 실행을 sandbox 기반 실행으로 교체
- DB repository를 실제 API 엔드포인트와 연결
- 인증 정책 확정
- 봇 제출 및 조회 API 추가
- 라이브 세션 결과를 경기 이력과 참가자 통계로 영속화

### **16.2 중기**

- 랭킹 및 리더보드 영속화
- 리플레이 및 분석 도구
- 향상된 프론트엔드 대시보드
- 운영 모니터링 강화

### **16.3 장기**

- PvE 보스 모드
- RL 기반 고도화 봇
- LLM 해설
- 팀전 및 확장 규칙셋

## **17. 미결정 사항**

다음 항목은 암묵적으로 넘기지 말고 명시적으로 추적해야 한다.

- API 경로에서 샌드박스 실행을 언제부터 의무화할지
- 인증 모델을 무엇으로 확정할지
    - JWT 전용
    - 세션 기반
    - OAuth 추가
- 봇 코드를 경기 단위로 제출할지, 먼저 영구 자산으로 관리할지
- Redis를 배포 필수 요소로 둘지, 추상화 뒤 선택 요소로 둘지
- 경기 이력을 v1.0 필수로 볼지, v1.1 마일스톤으로 미룰지

## **18. 문서 해석 원칙**

이전 브레인스토밍 문서와 이 문서가 충돌할 경우 다음 원칙을 따른다.

- 엔진 수치는 현재 `src/arena/config.py` 값을 기준으로 한다
- 게임 룰은 현재 엔진 동작을 기준으로 한다
- 브레인스토밍 수준의 아이디어는 여기서 명시적으로 확정하지 않은 이상 미래 범위로 본다

## **19. 요약**

LOA는 현재 기준으로 다음 특성이 가장 강한 AI Agents 플랫폼이다.

- 명확하고 결정적인 룰
- 부분 관측
- 게임성
- 실시간 관전

이 문서는 현재 통합 기준선 역할을 한다.

- 게임 룰 스펙은 엔진과 맞춰 고정한다
- 현재 코드 현실을 문서에 반영한다
- 계획 범위는 남기되, 이미 완성된 것처럼 쓰지 않는다