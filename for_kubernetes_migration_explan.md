# Kubernetes 전환 작업 — 쉬운 설명

> 이 문서는 `for_kubernetes_migration_plan.md`와 `for_kubernetes_checklist.md`의 내용을
> 비전공자도 이해할 수 있게 풀어서 설명한 것입니다.

---

## 한 줄 요약

**지금 쓰는 서버 환경(Cloud Run)에서 더 안전하고 확장 가능한 서버 환경(Kubernetes)으로 옮기는 작업입니다.**
이 과정에서 가장 중요한 목표는 **유저가 제출한 봇 코드를 서버로부터 완전히 격리해서 실행**하는 것입니다.

---

## 왜 이걸 해야 하나요?

### 현재 문제점

지금은 유저가 Python 봇 코드를 제출하면, 그 코드가 **게임 서버 내부에서 직접 실행**됩니다.

```
[게임 서버]
  └── DB 비밀번호가 여기 있음
  └── API 키가 여기 있음
  └── 유저가 제출한 봇 코드도 여기서 실행됨  ← 위험!
```

악의적인 유저가 봇 코드 안에 `os.environ` 같은 명령을 넣으면 서버의 비밀 정보를 훔칠 수 있습니다.

### 목표 상태

```
[게임 서버] — DB 비밀번호, API 키 보관
     │
     │ (HTTP 요청으로만 통신)
     ▼
[Bot Runner 서버] — 비밀 정보 없음, 인터넷 차단, 격리된 환경
  └── 유저 봇 코드가 여기서만 실행됨  ← 안전!
```

봇 코드는 아무것도 없는 격리된 공간에서만 실행되므로, 서버 정보를 훔칠 수 없습니다.

---

## 용어 정리

| 용어 | 쉬운 설명 |
|------|-----------|
| **Cloud Run** | 지금 쓰는 구글 서버 환경. 간단하지만 기능 제한이 있음 |
| **Kubernetes (GKE)** | 더 강력한 구글 서버 환경. 여러 서버를 세밀하게 제어 가능 |
| **Bot Runner** | 유저 봇 코드만 실행하는 전용 서버. 비밀 정보 없음 |
| **game-server** | 기존 게임 서버. DB, Redis, 비밀 정보 보유 |
| **gVisor** | Bot Runner에 적용하는 추가 격리 기술. 봇이 시스템 자원에 접근 못하게 막음 |
| **NetworkPolicy** | Bot Runner의 인터넷 연결을 완전 차단하는 방화벽 |
| **Ingress** | 외부에서 서버로 들어오는 입구 (도메인/IP 연결) |
| **ConfigMap / Secret** | 서버 설정값과 비밀번호를 저장하는 Kubernetes 방식 |
| **Workload Identity** | 서버가 GCS(파일 저장소)에 안전하게 접근하도록 연결해주는 인증 방식 |
| **Skaffold** | 로컬 Kubernetes(minikube)와 GKE를 프로파일로 분리해 자동 배포해주는 Google 오픈소스 도구. 무료 |
| **minikube** | 개발자 PC에서 Kubernetes를 로컬로 실행할 수 있게 해주는 도구. GKE 없이 테스트 가능 |
| **컨텍스트 (kubectl context)** | kubectl이 "지금 어느 클러스터에 명령을 보낼지" 기억하는 설정. 로컬↔GKE 전환에 사용 |

---

## 전체 흐름 (10단계)

### Phase 0 — 사전 점검 "지금 상태 파악하기"

> **비유:** 이사 가기 전에 집 목록 작성하기

- 지금 쓰는 GCP 리소스(DB 주소, Redis 주소, 저장소 이름 등)를 전부 확인
- 현재 서버가 정상 동작하는지 기록해둠 (나중에 비교용)
- GKE로 옮겼을 때 비용이 얼마나 나올지 예산 확인
- **결과물:** 현재 상태 기록, 이사 전 집 상태 스냅샷

---

### Phase 1 — Bot Runner 서비스 구현 "격리 실행 서버 만들기"

> **비유:** 위험한 실험을 전용 실험실에서만 하도록 분리하기

- `backend/BotRunner/` 폴더 안에 새 서버를 만듦
- 이 서버는 딱 하나의 역할만 함: **유저 봇 코드를 받아서 실행하고 결과를 돌려주기**
- 봇 코드가 위험한 짓(파일 읽기, 인터넷 접속, 환경변수 읽기)을 하면 자동으로 차단
- 봇이 너무 오래 실행되면 강제로 멈추고 기본값 반환
- 이 단계에서는 아직 실제 게임 서버와 연결 안 함 → **PR 1로 제출**

---

### Phase 2 — RemoteBotAdapter 연결 "게임 서버와 Bot Runner 이어주기"

> **비유:** 본관 건물에서 위험한 실험이 필요할 때 실험실 건물로 요청 보내기

- 게임 서버가 봇을 실행할 때, 직접 실행하지 않고 Bot Runner에 HTTP 요청을 보냄
- Bot Runner가 실행 결과를 돌려주면 게임 서버가 그걸 사용
- `BOT_RUNNER_URL` 환경변수가 없으면 기존 방식(직접 실행)으로 폴백 → 로컬 개발 가능
- 서버 상태 확인 엔드포인트 2개 추가:
  - `/healthz` — DB 연결 포함 전체 상태 확인 (문제 있으면 503)
  - `/livez` — 서버가 살아있는지만 확인 (항상 200)
- → **PR 2로 제출**

---

### Phase 3 — Kubernetes 매니페스트 작성 "설계도 그리기"

> **비유:** 새 아파트 평면도 그리기. 아직 짓지는 않음

- `k8s/` 폴더에 YAML 파일들을 작성 (서버 구성 설계도)
- 무엇을 정의하냐:
  - game-server: replica 1개 (세션 유지 때문에 반드시 1개)
  - bot-runner: replica 2개, gVisor 격리, 비밀 정보 없음, 인터넷 차단
  - 로드밸런서, 도메인 연결, WebSocket 타임아웃 설정
- 이 단계에서는 실제 서버를 만들지 않음. 설계도만 → **PR 3으로 제출**

---

### Phase 4 — GKE 클러스터 생성 "실제 서버 환경 만들기"

> **비유:** 설계도 대로 실제 건물 짓기 (GCP에서 클릭/명령어로)

- GCP에서 Kubernetes 클러스터를 생성
- 기존 DB, Redis와 같은 네트워크 안에 만들어서 연결 가능하게 함
- gVisor가 제대로 동작하는지 테스트 (봇 격리 핵심 기술)
- 이 단계는 코드 PR 없음. GCP 운영 작업

---

### Phase 5 — 이미지 빌드 및 배포 파이프라인 "자동 배포 설정"

> **비유:** 코드를 수정할 때마다 자동으로 두 건물(Cloud Run + GKE) 모두 업데이트

- Docker 이미지를 빌드해서 GKE에 올리는 방법 정리
- Cloud Build 자동화 파이프라인에 GKE 배포 단계 추가
- **중요:** 이 단계에서도 기존 Cloud Run은 살려둠. 두 서버가 동시에 운영됨
  - Cloud Run: 기존 방식(직접 봇 실행), 실제 사용자 트래픽 처리
  - GKE: 새 방식(Bot Runner 사용), 개발자가 내부 검증용으로만 사용
- **Skaffold 로컬 개발 환경 설정 (선택)**
  - 로컬 Kubernetes(minikube)와 GKE를 프로파일로 분리해서 자동 전환
  - `skaffold dev --profile=local` → 코드 수정 시 자동 재빌드/재배포 (로컬)
  - `skaffold run --profile=production` → GKE에 배포
  - 팀원 전원이 쓸 필요는 없음. 인프라 담당자 또는 매니페스트 자주 수정하는 팀원에게 유용
  - Skaffold 자체는 무료. 로컬 minikube 테스트도 무료. GKE 배포 시에만 GCP 비용 발생
- → **PR 4 (수동 배포 + Skaffold 선택), PR 5 (자동 배포)**

---

### Phase 6 — Secret과 권한 설정 "열쇠 관리"

> **비유:** 건물 입주 후 어느 방에 어느 열쇠를 줄지 결정하기

- game-server만 DB 비밀번호, API 키를 가짐
- bot-runner는 아무 비밀 정보도 없음
- GCS(파일 저장소)에서 보스 봇 가중치를 읽는 권한: game-server에만 부여
- Workload Identity로 안전하게 연결

---

### Phase 7 — gVisor Smoke Test "격리가 실제로 작동하는지 확인"

> **비유:** 새 실험실 안에서 실제로 실험 장비가 잘 작동하는지 테스트

- 실제로 배포된 bot-runner Pod 안에서 Python 격리 기능 테스트:
  - 프로세스 분리 (`multiprocessing`) 작동 확인
  - CPU/메모리 제한 작동 확인
  - 타임아웃 작동 확인
  - 임시 파일 쓰기 작동 확인
- 실패하면 → gVisor 대신 다른 격리 방법으로 전환

---

### Phase 8 — 통합 테스트 "전체 시나리오 검증"

> **비유:** 새 건물에서 실제 입주자처럼 모든 기능 테스트하기

- 로그인 → 봇 제출 → 게임 생성 → 관전(WebSocket) → 종료 → 결과 확인
- BattleRoyale, Boss, MockStocks 모두 테스트
- Cloud Run URL과 GKE URL 양쪽 모두 정상 작동하는지 확인
- 봇 코드에서 서버 비밀 정보가 새지 않는지 확인

---

### Phase 9 — 안정화 관찰 "안정적으로 돌아가는지 며칠 지켜보기"

> **비유:** 이삿짐 다 옮겼지만 아직 구 집(Cloud Run)은 계약 유지해둠

- GKE에서 게임이 안정적으로 돌아가는지 수일 관찰
- 비용이 예산을 넘지 않는지 모니터링
- Cloud Run도 동시에 계속 정상 동작하는지 확인 (롤백 대비)
- → **PR 6**

---

### Phase 10 — Frontend/API Cutover "사용자 트래픽 전환"

> **비유:** 구 집 계약 만료 전에 모든 짐을 새 집으로 완전히 옮기기

- 팀 전체 승인 후 진행
- Firebase 설정에서 API 주소를 GKE 주소로 변경
- 실제 사용자 트래픽이 GKE로 가기 시작
- Cloud Run은 삭제하지 않고 롤백 대비용으로 유지
- → **PR 7**

---

## 전체 타임라인 요약

```
현재
  │
  ▼
Phase 0  현재 상태 점검
  │
  ▼
Phase 1  Bot Runner 서버 만들기              (PR 1)
  │
  ▼
Phase 2  게임 서버와 Bot Runner 연결          (PR 2)
  │
  ▼
Phase 3  Kubernetes 설계도 작성              (PR 3)
  │
  ▼
Phase 4  실제 GKE 서버 생성                 (GCP 작업)
  │
  ▼
Phase 5  자동 배포 파이프라인 설정            (PR 4, PR 5)
  │      ← 이 시점부터 Cloud Run + GKE 병행 운영
  ▼
Phase 6  권한/비밀 정보 설정
  │
  ▼
Phase 7  gVisor 격리 검증
  │
  ▼
Phase 8  전체 E2E 테스트
  │
  ▼
Phase 9  수일 안정화 관찰                    (PR 6)
  │
  ▼
Phase 10 사용자 트래픽 GKE로 완전 전환        (PR 7)
  │
  ▼
완료: Cloud Run은 백업용으로 남겨둠
```

---

## 핵심 안전 원칙

1. **절대 한 번에 다 바꾸지 않는다** — 단계별로 쪼개서 문제가 생기면 바로 원인을 알 수 있게
2. **Cloud Run은 Phase 10 이전까지 절대 삭제하지 않는다** — 문제 발생 시 즉시 되돌릴 수 있게
3. **Bot Runner에는 비밀 정보를 절대 주지 않는다** — 유저 봇 코드가 훔칠 게 없게
4. **봇 코드는 외부 인터넷에 접근할 수 없게 막는다** — 데이터 유출 경로 차단

---

## 현재 진행 상태 (2026-05-19 기준)

- **Phase 0**: 거의 완료 (WORK_LOG.md 기록, gVisor 확인, 예산 계산 일부 남음)
- **Phase 1**: 완료 ✅ (Bot Runner 구현 및 테스트 완료, 78개 테스트 통과)
- **Phase 2 이후**: 미시작
