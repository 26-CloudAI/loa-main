# ── Node / Frontend ──────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Python / Backend ─────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
build/
.venv/
venv/
.pytest_cache/

# ── Database ─────────────────────────────────────
*.db
*.sqlite3

# ── 환경변수 ──────────────────────────────────────
.env
.env.*
!.env.example

# ── 시크릿 / 인증 키 (절대 커밋 금지) ─────────────
*.pem
*.p12
*.key
*.pfx
*service-account*.json
*service_account*.json
*-credentials.json
*_credentials.json
*-key.json
*_key.json
firebase-adminsdk*.json
gcloud-key*.json
gemini_key.py
secrets/*
!secrets/*.example.json
!secrets/*.example.py

# ── 학습 가중치 (GCS로 관리) ──────────────────────
backend/BattleRoyale/bots/trained_weights.json
backend/BattleRoyale/bots/trained_weights.pt
backend/BattleRoyale/bots/boss/trained_weights.json
backend/BattleRoyale/bots/boss/trained_weights.pt
backend/BattleRoyale/bots/*.bak*
backend/BattleRoyale/bots/quality_bots.json

# ── 로그 ─────────────────────────────────────────
logs/
*.log

# ── Firebase ─────────────────────────────────────
.firebase/

# ── Docker ───────────────────────────────────────
docker-compose.override.yml
temp_bots/

# ── IDE ──────────────────────────────────────────
.vscode/
.idea/
*.iml
*.swp
*.suo
*.sw?

# ── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db

# ── 기타 ─────────────────────────────────────────
wiki/
.claude/
CLAUDE.md
